(function(){const bell=document.querySelector('[data-notification-bell]'),count=document.querySelector('[data-notification-count]');async function json(url,options){const response=await fetch(url,{...options,headers:{'content-type':'application/json',...(options||{}).headers}}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Notification request failed.');return data}async function refresh(){try{const data=await json('/api/notifications?limit=1');if(count){count.textContent=data.unread||'';count.hidden=!data.unread}if(bell)bell.setAttribute('aria-label',`Notifications${data.unread?`, ${data.unread} unread`:''}`)}catch{}}window.PortalNotifications={json,refresh,async enable(){if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window))throw new Error('Web Push is not supported by this browser.');const permission=await Notification.requestPermission();if(permission!=='granted')throw new Error('Notification permission was not granted.');const key=(await json('/api/push/public-key')).publicKey;if(!key)throw new Error('Push notification keys are not configured on the server.');const registration=await navigator.serviceWorker.register('/service-worker.js'),padding='='.repeat((4-key.length%4)%4),bytes=Uint8Array.from(atob((key+padding).replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0)),subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes});await json('/api/push/subscribe',{method:'POST',body:JSON.stringify({subscription:subscription.toJSON(),device_name:`${navigator.userAgentData?.brands?.[0]?.brand||'Browser'} on ${navigator.userAgentData?.platform||navigator.platform||'device'}`,browser:navigator.userAgentData?.brands?.[0]?.brand||'',platform:navigator.userAgentData?.platform||navigator.platform||''})});return true}};refresh();setInterval(refresh,60000);if('Notification'in window&&Notification.permission==='default'&&!localStorage.getItem('portal-push-prompt-dismissed'))setTimeout(()=>{const prompt=document.createElement('aside');prompt.className='notification-optin';prompt.innerHTML='<strong>Stay updated</strong><span>Receive calendar and reminder notifications on this device.</span><div><button class="btn" data-enable>Enable Notifications</button><button class="btn secondary" data-dismiss>Not Now</button></div>';document.body.appendChild(prompt);prompt.querySelector('[data-enable]').onclick=async()=>{try{await window.PortalNotifications.enable();prompt.remove()}catch(error){prompt.querySelector('span').textContent=error.message}};prompt.querySelector('[data-dismiss]').onclick=()=>{localStorage.setItem('portal-push-prompt-dismissed','1');prompt.remove()}},1200)})();
